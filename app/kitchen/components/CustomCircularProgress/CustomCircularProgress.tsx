import DefaultProgress from "../../../../components/CircularProgress"

export default function CustomCircularProgress() {
    return (
        <div className="flex h-screen border justify-center items-center">
            <DefaultProgress />
        </div>
    )
}
